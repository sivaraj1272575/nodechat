.messages
    each type in Object.keys(messages)
        each message in messages[type]
            div(class= "alert alert-"+type+" alert-dismissible")
                a.close(href = "#" data-dismiss="alert" aria-label="close") x
                p #{message}